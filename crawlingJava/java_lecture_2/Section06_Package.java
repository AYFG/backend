/**
 * ## 섹션 6. 패키지
 */
public class Section06_Package {
    public static void main(String[] args) {

        // 1. [강의: 패키지 - 시작]
        /* 
         * 패키지는 컴퓨터의 '폴더'와 같습니다. 
         * 클래스들을 연관된 것끼리 그룹핑하여 관리합니다.
         */

        // 2. [강의: 패키지 - import]
        /* 다른 패키지에 있는 클래스를 사용할 때는 import 문을 사용해야 합니다. */
        // java_basic_curriculum.SomeClass some = new java_basic_curriculum.SomeClass(); // 전체 경로 사용
        // import java_basic_curriculum.SomeClass; // 상단에 선언 후 사용 (추천)

        // 3. [강의: 패키지 규칙]
        /* 
         * - 패키지의 이름과 위치가 폴더 구조와 반드시 일치해야 합니다.
         * - 패키지 이름은 모두 소문자를 사용하는 관례가 있습니다.
         */
    }
}
